namespace Acme.Services.VoucherManagementService.Application.Handlers.RedeemVoucher;

public class RedeemVoucherResponse
{
    // This response is intentionally empty as the redemption operation
    // doesn't need to return any data on success. The HTTP status code
    // is sufficient to indicate success.
}
